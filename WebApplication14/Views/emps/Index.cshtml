@model IEnumerable<WebApplication14.Models.emp>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table dropdown-menu-right table-bordered table-responsive" style="background-color:burlywood;">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.salary)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)

    {
<tr>
    <td>
        @Html.DisplayFor(modelItem => item.name)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.salary)
    </td>
    
    <td>
        <a href="@Url.Action("Details","emps",new { item.id })"><button class="btn btn-success">Details</button></a>
    </td>


    <td>
        <a class="btn btn-warning" href="@Url.Action("edit", "emps", new {item.id})">Edit</a>
    </td>
    <td>
        <a href="@Url.Action("Delete","emps",new { item.id })"><button class="btn btn-danger">delete</button></a>
    </td>

</tr>
        }


    </table>
